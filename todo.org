#+TITLE: Tag Project Todo List
#+PROPERTY: LOGGING nil

* DONE TagID [5/5]

The alphanumeric ID of the tag system.

** DONE TagID struct

Implementation of tag ID.

** DONE TagID from(str) trait

Generate ID from the given string literal.

** DONE TagID display trait

Display the ID.

** DONE TagID generate_next(TagID) fn

Generate a tag ID that should come after the given ID. For example, @1.0 -> @2.0

** DONE TagID generate_between(TagID, TagID) fn

Generate a tag ID between two given IDs. For example, (@1.0, @2.0) -> @1.1

* DONE TagField [4/4]

Field of an item. Contains the field string for all valid types.

** DONE TagField enum

Enum for different types of tag fields. 

** DONE TagField from(str) trait

Generate tag field from the given string literal.

** DONE TagField flags fn

Fetch all the flags if the field is a flags field.

** DONE TagField attribute fn

Return attribute and its value if the field is an attribute field.

* DONE TagItem [8/8]

Tag item. Contains fields. Must be given an ID field.

** DONE TagItem struct

Implementation of tag item structure.

** DONE TagItem from(str) trait

Generate tag item from the given string literal.

** DONE TagItem display trait

Display trait for tag item.

** DONE TagItem fields fn

Return a vector of all the fields in the item.

** DONE TagItem flags fn

Return all the flags of the item.

** DONE TagItem attributes fn

Return all the attributes of the item.

** DONE TagItem fetch functions [4/4]

Check for flags and attributes in an item.

*** DONE TagItem has_flag(flag) fn

Return true if the item has the flag, otherwise return false.

*** DONE TagItem fetch_attribute(attribute_key) fn

Return the (key, value) attribute if the item has it, otherwise return None.

*** DONE TagItem has_attribute(attribute_key) fn

Return true if the item has the attribute, otherwise return false.

*** DONE TagItem match_attribute(key, value) fn

Return true if the item has the attribute and the key-value matches, otherwise return false.

** DONE TagItem modification functions [4/4]

Modify the item.

*** DONE TagItem add_flag(flag) fn

Add flag to the item.

*** DONE TagItem remove_flag(flag) fn

Remove flag from the item.

*** DONE TagItem set_attribute(key, value) fn

Set the attribute of the item.

*** DONE TagItem remove_attribute(key) fn

Remove the attribute of the item.

* TODO TagList [5/8]

A structure to hold multiple tag items. Should provide fast single and sequential look-ups of given tag ID(s). Also, provide fast querying of items that have certain flags and/or attribute.

** DONE TagList struct

Structure for tag list.

** TODO TagList insert(TagItem) fn

Insert an item to the tag list.

** DONE TagList access(TagID) fn

Check/access a tag item.

** DONE TagList where_flag(flag) fn

Return an iterator of all the tag items in the list that has the given flag.

** DONE TagList where_attribute(key, value) fn

Return an iterator of all the tag items in the list that has attribute set to the given flag.

** DONE TagList exclude_flag(flag) fn

Exclude all the tag items in the list that has the given flag.

** TODO TagList exclude_attribute(key, value) fn

Exclude all the tag items in the list that has attribute set to the given flag.

** TODO TagList modify(TagID, modification) fn

Modify an item in the list.

* TODO File I/O [1/4]

Handling Tagfile.

** DONE Read a Tagfile

Read TagList from the disk.

** TODO Write to Tagfile

Write TagList back to the disk.

** TODO Helper file

A binary file that's used to speed up tag operation.

** TODO Syncronize Tagfile

If the Tagfile was manually modified, syncronize the helper file.

* TODO Command-line interface [1/5]

Command-line commands.

** TODO "generate" commands [0/2]

For experimenting with TagID. Lets you run generate functions from the CLI.

*** TODO generate next (TagID) command

Given a TagID as argument or standard input, generate the next one.

*** TODO generate between (TagID, TagID) command

Given two TagID as arguments or standard input, generate one between them.

** TODO filter commands [1/2]

Given a range of conditions, return the TagID of items that match them all.

*** DONE with-flag(s) command

Must have the given flag(s).

*** TODO attribute: value rule

Must have the attribute set to the given value.

** TODO filter exclude commands [1/2]

Given a range of conditions, exclude the TagID of items that match those conditions.

*** DONE without-flags(s) command

Exclude items with the given flag(s).

*** TODO attribute: value exclusion rule

Exclude items that has attribute set to the given value.

** DONE "search" command

Given a list of TagID as argument or standard input, print out their taglines.

** TODO "insert" commands [0/4]

Insert new items to the list.

*** TODO insert TagID command

Generate and insert a new item with the given TagID to its appropriate position.

*** TODO insert first command

Generate and insert a new item at the beginning of the list with an appropriate TagID.

*** TODO insert last command

Generate and insert a new item at the end of the list with an appropriate TagID.

*** TODO insert after command

Generate and insert a new item after a given existing item with an appropriate TagID.
